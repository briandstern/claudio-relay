[phases.setup]
apt_pkgs = ["fonts-liberation", "fonts-dejavu-core", "fontconfig"]

[phases.build]
cmds = ["PLAYWRIGHT_BROWSERS_PATH=0 python -m playwright install chromium --with-deps"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
